function mov(ops)   { return `mov` }
function movb(ops)  { return `movb` }
function cmp(ops)   { return `cmp` }
function cmpb(ops)  { return `cmpb` }
function bit(ops)   { return `bit` }
function bitb(ops)  { return `bitb` }
function bic(ops)   { return `bic` }
function bicb(ops)  { return `bicb` }
function bis(ops)   { return `bis` }
function bisb(ops)  { return `bisb` }
function add(ops)   { return `add` }
function sub(ops)   { return `sub` }
function mul(ops)   { return `mul` }
function div(ops)   { return `div` }
function ash(ops)   { return `ash` }
function ashc(ops)  { return `ashc` }
function xor(ops)   { return `xor` }
function jmp(ops)   { return `jmp` }
function swab(ops)  { return `swab` }
function clr(ops)   { return `clr` }
function clrb(ops)  { return `clrb` }
function com(ops)   { return `com` }
function comb(ops)  { return `comb` }
function inc(ops)   { return `inc` }
function incb(ops)  { return `incb` }
function dec(ops)   { return `dec` }
function decb(ops)  { return `decb` }
function neg(ops)   { return `neg` }
function negb(ops)  { return `negb` }
function adc(ops)   { return `adc` }
function adcb(ops)  { return `adcb` }
function sbc(ops)   { return `sbc` }
function sbcb(ops)  { return `sbcb` }
function tst(ops)   { return `tst` }
function tstb(ops)  { return `tstb` }
function ror(ops)   { return `ror` }
function rorb(ops)  { return `rorb` }
function rol(ops)   { return `rol` }
function rolb(ops)  { return `rolb` }
function asr(ops)   { return `asr` }
function asrb(ops)  { return `asrb` }
function asl(ops)   { return `asl` }
function aslb(ops)  { return `aslb` }
function mtps(ops)  { return `mtps` }
function mfpi(ops)  { return `mfpi` }
function mfpd(ops)  { return `mfpd` }
function mtpi(ops)  { return `mtpi` }
function mtpd(ops)  { return `mtpd` }
function sxt(ops)   { return `sxt` }
function mfps(ops)  { return `mfps` }
function br(ops)    { return `br` }
function bne(ops)   { return `bne` }
function beq(ops)   { return `beq` }
function bge(ops)   { return `bge` }
function blt(ops)   { return `blt` }
function bgt(ops)   { return `bgt` }
function ble(ops)   { return `ble` }
function bpl(ops)   { return `bpl` }
function bmi(ops)   { return `bmi` }
function bhi(ops)   { return `bhi` }
function blos(ops)  { return `blos` }
function bvc(ops)   { return `bvc` }
function bvs(ops)   { return `bvs` }
function bcc(ops)   { return `bcc` }
function bhis(ops)  { return `bhis` }
function bcs(ops)   { return `bcs` }
function blo(ops)   { return `blo` }
function sob(ops)   { return `sob` }
function jsr(ops)   { return `jsr` }
function rts(ops)   { return `rts` }
function emt(ops)   { return `emt` }
function trap(ops)  { return `trap` }
function halt(ops)  { return `halt` }
function wait(ops)  { return `wait` }
function rti(ops)   { return `rti` }
function bpt(ops)   { return `bpt` }
function iot(ops)   { return `iot` }
function reset(ops) { return `reset` }
function clc(ops)   { return `clc` }
function clv(ops)   { return `clv` }
function clz(ops)   { return `clz` }
function cln(ops)   { return `cln` }
function sec(ops)   { return `sec` }
function sev(ops)   { return `sev` }
function sez(ops)   { return `sez` }
function sen(ops)   { return `sen` }
function scc(ops)   { return `scc` }
function ccc(ops)   { return `ccc` }
function nop(ops)   { i return `ccc` `}
